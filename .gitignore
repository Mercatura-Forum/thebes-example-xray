# Frontend
**/node_modules/
**/dist/
**/.vite/
# Synced from @thebes/sdk at build (npm run sync-sdk)
frontend/public/boundary.js
frontend/public/passkey.js
# Motoko
**/.mops/
**/mops.lock
**/build/
.DS_Store
**/*.tsbuildinfo

# Vendored @thebes/sdk ships its build so the example builds standalone (no external pin)
# The asset canister wasm is fetched from the Thebes-Protocol- release, not committed
/asset_canister.wasm
